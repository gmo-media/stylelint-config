{
  "name": "@gmo-media/stylelint-config",
  "description": "stylelint config for gmo-media.",
  "version": "2.1.0",
  "author": "GMO Media,Inc.",
  "devDependencies": {
    "stylelint": "^8.3.1"
  },
  "files": [
    "*.js",
    "README.md"
  ],
  "license": "MIT",
  "main": "regular.js",
  "peerDependencies": {
    "stylelint": "^8.3.1"
  },
  "repository": "https://github.com/gmo-media/stylelint-config",
  "scripts": {
    "test": "stylelint --config test/stylelintrc.json --syntax scss --fix test/*.scss"
  },
  "dependencies": {
    "stylelint-config-standard": "^18.0.0"
  }
}
